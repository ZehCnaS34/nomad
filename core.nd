(def *ns* nil)

(def *ns* 'nomad.core)

(def inc (fn [value] (+ value 1)))
(def dec (fn [value] (- value 1)))

(println *ns*)

(def fact
  (fn [n]
    (println n)
    (if (= n 1)
      n
      (* n (fact (dec n))))))

(println (fact 10))


