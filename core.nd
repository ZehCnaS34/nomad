(def inc (fn [value] (+ value 1)))
(def dec (fn [value] (- value 1)))

(def fact
  (fn [n]
    (println n)
    (if (= n 1)
      n
      (* n (fact (dec n))))))

(println (fact 10))

(println "alex")

(println "alexander \"xela\" sanchez")

name.age.wow/*core*
